# Placeholder — NO es la configuración real en uso.
#
# El contenedor de proxy (linuxserver/swag) gestiona su propia configuración
# interna de nginx y actualmente NO monta este archivo (ver
# proxy/docker-compose.yml — el único bind mount es ${BASE_DIR}:/config,
# apuntando hoy a /home/leojimenezcr/proxy en el host, fuera de este repo).
#
# Este archivo queda como placeholder para cuando se migre la configuración
# real de SWAG a este repo (paso manual, por SSH, fuera de esta
# reorganización — ver proxy/README.md). No se inventó contenido real acá.
