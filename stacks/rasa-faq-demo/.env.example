# Variables esperadas por stacks/rasa-faq-demo/docker-compose.yml
# Cargar los valores reales en Portainer > Stack "rasa-faq-demo" > Environment variables.
# NUNCA completar este archivo con valores reales ni versionarlo en git con datos sensibles.

# Carpeta raíz de este stack en el host; se monta completa como /app (rasa)
# y su index.html se monta individualmente en webchat.
BASE_DIR=/ruta/al/volumen/de/rasa-faq-demo
