# Variables esperadas por stacks/applio/docker-compose.yml
# Cargar los valores reales en Portainer > Stack "applio" > Environment variables.
# NUNCA completar este archivo con valores reales ni commitearlo con datos sensibles.

APPLIO_MODELS_DIR=/ruta/al/volumen/de/applio/models
APPLIO_OUTPUTS_DIR=/ruta/al/volumen/de/applio/outputs
