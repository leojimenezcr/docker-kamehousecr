# Variables esperadas por stacks/applio/docker-compose.yml
# Cargar los valores reales en Portainer > Stack "applio" > Environment variables.
# NUNCA completar este archivo con valores reales ni versionarlo en git con datos sensibles.

# Carpeta raíz de este stack en el host; contiene models/ y outputs/.
BASE_DIR=/ruta/al/volumen/de/applio
