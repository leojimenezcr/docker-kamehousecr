# Variables esperadas por stacks/piper/docker-compose.yml
# Cargar los valores reales en Portainer > Stack "piper" > Environment variables.
# NUNCA completar este archivo con valores reales ni commitearlo con datos sensibles.

# Carpeta raíz de este stack en el host; contiene models/.
BASE_DIR=/ruta/al/volumen/de/piper
